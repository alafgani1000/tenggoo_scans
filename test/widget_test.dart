import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — app uses native plugins that require a device
    expect(true, isTrue);
  });
}
